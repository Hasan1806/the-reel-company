# ==========================================================
# The Reel Company — Environment Variables Configuration
# ==========================================================

# Production Canonical Site URL (Used for Sitemap, Robots, Open Graph)
NEXT_PUBLIC_SITE_URL=https://www.thereelcompany.in

# Server Port (Hostinger Web App default is 3000 or custom PORT)
PORT=3000

# Node Environment
NODE_ENV=production
